# Example environment variables for Prisma (Neon Postgres)
# Copy this file to `.env` and replace with your actual connection strings.

# Pooled connection string (used by the application at runtime)
DATABASE_URL="postgresql://user:password@host:port/dbname?pgbouncer=true&connection_limit=1&connect_timeout=10"

# Unpooled/direct connection string (used by prisma migrate or long-running ops)
# NOTE: Our schema expects DATABASE_URL_UNPOOLED for `directUrl = env("DATABASE_URL_UNPOOLED")`
DATABASE_URL_UNPOOLED="postgresql://user:password@host:port/dbname?connect_timeout=10"

# Optional app flags (safe defaults for local/dev)
NEXT_TELEMETRY_DISABLED=1
WA_DRY_RUN=true
CHATRACE_ENABLED=false

# WhatsApp Cloud API (server-only; leave unset locally unless needed)
# WhatsApp business account id (WABA)
# WHATSAPP_WABA_ID=636637465972102
# Phone number ID (used in Graph send endpoint)
# WHATSAPP_PHONE_NUMBER_ID=953499351183719
# Bearer token for Graph API calls (use a long-lived token)
# WHATSAPP_TOKEN=
# App secret (used to validate X-Hub-Signature-256)
# WHATSAPP_APP_SECRET=
# Verify token for webhook GET challenge
# IMPORTANT: In production set this env exactly as provided to Meta when registering the webhook.
# Example (store in your deployment environment, do NOT commit secrets):
# WHATSAPP_VERIFY_TOKEN=barakaops_wa_verify_9fK3pQm2xT7vL8nR

# Safaricom Daraja (optional)
# Daraja / Safaricom settings
# Required for STK/C2B flows. For production use the live API base below.
# DO NOT commit production secrets into source control. Use your deployment env (Vercel) to store values.
# Production base (set when running in live mode)
# DARAJA_BASE_URL=https://api.safaricom.co.ke
# Daraja credentials (from Safaricom - keep secret)
# DARAJA_CONSUMER_KEY=your_consumer_key_here
# DARAJA_CONSUMER_SECRET=your_consumer_secret_here
# DARAJA_C2B_SHORTCODE=your_c2b_shortcode_here
# DARAJA_PASSKEY_HO=your_passkey_here
# Optional: per-till/passkey overrides
# DARAJA_PASSKEY_<SHORTCODE>=...
# Optional MPESA API user/password (some integrations provide portal user creds)
# DARAJA_API_USER=your_api_user_here
# DARAJA_API_PASSWORD=your_api_password_here
# PUBLIC_BASE_URL=https://example.com
# Feature toggles
# WA_DARAJA_ENABLED=true
# DARAJA_LIVE_MODE=false
