// wa_tabs removed — legacy tab helper replaced by GPT-first messaging.
export async function sendCanonicalTabs() {
  // no-op: keep function present for compatibility but do nothing so GPT composes messages instead
  return;
}

export default {};
